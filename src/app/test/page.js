// page.js (이름은 고정값) 로 페이지를 그린다.
// layout.js로 전체적인 틀을 그린다.
// 폴더로 라우팅을 한다.
const TestPage = () => {
    return (
        <>
            <h1>TestPage</h1>
        </>
    )
}

export default TestPage;